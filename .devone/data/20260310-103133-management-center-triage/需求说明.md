# 需求说明

## 文档元信息

- 任务名称: management-center-triage
- 任务目录: .devone/data/20260310-103133-management-center-triage
- 创建时间: 20260310-103133
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: discovery
- 当前状态: draft

## 问题定义

- 当前问题: 你已启动 DevOne，但尚未给出本次要交付的具体功能/修复目标；需要先把目标、范围与成功标准收敛清楚，避免无关改动。
- 影响对象: 本仓库维护者与管理中心使用者（具体受影响页面/功能待你确认）。
- 触发背景:
  - 当前分支: `gdtiti-plus`
  - 当前工作区存在未提交改动: `.mono/monoConfig.json` 新增 `frontend` 路径映射（是否应纳入交付待确认）。
- 为什么现在处理: 未明确目标就进入 execution 容易产生“看起来改了但不可验收”的脏改动；需要先锁定执行模式与交付面，再进入实现与验收。

## 目标与成功标准

- 目标 1: 明确“这次要做什么 / 不做什么”，并给出可观察的成功标准（页面、配置、构建产物或行为）。
- 目标 2: 产出并获得你批准的设计与分阶段计划（含最小验证命令与证据要求），之后再进入实现。
- 成功标准:
  - [DISC] `需求说明.md` / `设计书.md` / `任务跟踪列表.md` / `单元测试设计文档.md` 已回写且你确认范围
  - [EXEC] 至少一个可验证结果成立（例如 `pnpm run lint` 或 `pnpm run build` fresh evidence）

## 范围边界

### 本次必须完成

- 明确执行模式：`full` 或 `required-only`（当前为代理默认 `required-only`，待你确认）
- 明确本次交付目标与成功标准
- 明确 `.mono/monoConfig.json` 的 `frontend` 映射是否属于本次交付：保留并完善 / 回滚 / 另开任务
- 形成可批准的设计与分阶段计划（含最小验证命令）

### 本次明确不做

- 未经批准的跨模块大重构
- 与本次目标无关的 UI/文案调整
- 大范围依赖升级 / 全量格式化（除非你明确要求）

## 验收真相陈述

- [REQ-001] 本次交付目标、成功标准、范围边界与非目标已明确，并记录在 `需求说明.md`。
- [REQ-002] `.mono/monoConfig.json` 改动去留已确认，并有对应说明与证据（diff/命令/结果）。
- [REQ-003] 若进入实现阶段：至少一条验证命令的 fresh evidence 已记录（例如 `pnpm run lint` / `pnpm run build`）。

## 约束与依赖

- 技术约束:
  - 前端栈: React + TypeScript + Vite（参考 `llmdoc/overview/project-overview.md`）
  - 当前环境: Windows + PowerShell
- 业务约束: 未经你明确批准，不引入“静默 fallback / 占位逻辑 / 假数据路径”。
- 外部依赖:
  - 若涉及后端能力验证: 依赖 CLI Proxy API 可用且可访问
  - 若涉及 `.mono/monoConfig.json`: 依赖实际消费该配置的工具/脚本（当前仓库内未检索到直接引用，需你确认用途）

## 风险与待确认项

- 风险:
  - 目标不明确会导致改动不可验收、难以提交或回滚
  - 工作区已有未提交改动，后续容易混入无关文件
- 待确认:
  - 你希望本次 DevOne 交付的具体目标是什么？（一句话即可）
  - 执行模式是否需要切换为 `full`？
  - `.mono/monoConfig.json` 的 `frontend` 映射是否要保留并提交到仓库？

## 模式说明

- 本轮默认按 `required-only` 推进（代理假设）。你确认后可随时切换到 `full` 并同步任务包。
