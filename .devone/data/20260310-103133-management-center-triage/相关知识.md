# 相关知识

## 文档元信息

- 任务名称: management-center-triage
- 任务目录: .devone/data/20260310-103133-management-center-triage
- 创建时间: 20260310-103133
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: discovery
- 当前状态: draft

## 已知事实

- 现有实现:
  - 管理中心为 React + TypeScript + Vite 前端（入口 `src/main.tsx` / `src/App.tsx`）
  - 可用脚本: `pnpm run dev` / `pnpm run build` / `pnpm run lint` / `pnpm run type-check`
- 相关文档:
  - `llmdoc/index.md`（文档阅读顺序）
  - `llmdoc/overview/project-overview.md`（项目定位与入口）
- 历史约束:
  - 本轮尚未确认与历史改动/分支策略的关系；先按当前工作区事实推进

## 术语与定义

- DevOne 任务包: `.devone/data/<timestamp>-<task-name>/` 下的交付文档集合
- `required-only`: 只强制推进必备任务；可选任务记录但默认不推进
- `full`: 同时推进必备任务与可选任务
- Wave: 一次可验证的小步（用于控制风险与证据收集）

## 调研结论

- 结论 1: 本仓库原先不存在 `.devone/`，已在本轮创建任务包 `.devone/data/20260310-103133-management-center-triage` 并进入 discovery / wave-1。
- 结论 2: 当前工作区存在未提交改动：`.mono/monoConfig.json` 新增 `frontend` 映射，且存在 “No newline at end of file” 提示（需确认是否要纳入交付）。
- 结论 3: 仓库内未检索到对 `.mono/monoConfig.json` 的直接引用（可能由外部工具消费，需确认）。

## 风险假设

- 假设: `.mono/monoConfig.json` 的改动确实是本次要交付的内容。
- 验证方式:
  - 你确认该配置文件的消费方/用途
  - 我们再按消费方验证新增映射是否必要且不会引入环境耦合

## 待补证据

- 需要你补充: 本次要交付的具体目标 / 成功标准（一句话即可）
- 若进入实施: 需要 fresh evidence（至少一次 `pnpm run lint` / `pnpm run build` 输出）
