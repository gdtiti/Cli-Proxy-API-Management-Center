# 技术说明

## 文档元信息

- 任务名称: i18n丢失-配额监控-多客户端登录记忆
- 任务目录: .devone/data/20260310-181815-i18n丢失-配额监控-多客户端登录记忆
- 创建时间: 20260310-181815
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: acceptance
- 当前状态: in_progress

## 实际改动摘要

- 本轮改动:
  - i18n：补齐侧边栏 `nav.monitor`，并新增 `client_selector.*` / `client_management.*` 文案（en/zh-CN/ru）。
  - 登录页：接入多客户端“记忆 + 管理”入口（选择已保存客户端后自动填充 `apiBase` / `managementKey`）。
  - Header：新增登录后快速切换入口（右上角下拉直接切换，不弹确认）；切换成功后刷新配置并提示。
  - 401 处理：Axios 客户端支持请求级 `suppressUnauthorizedEvent`，切换探测请求抑制全局 `unauthorized -> logout`，保证切换失败不强制登出。
  - Auth Files/Config Panel：补齐 ru 缺失文案（batch download / reload from store），并补齐“大数量导出”提示文案。
- 涉及文件/模块:
  - `src/services/api/client.ts` / `src/services/api/config.ts`
  - `src/stores/useAuthStore.ts` / `src/stores/useConfigStore.ts`
  - `src/pages/LoginPage.tsx` / `src/components/layout/MainLayout.tsx`
  - `src/i18n/locales/en.json` / `src/i18n/locales/zh-CN.json` / `src/i18n/locales/ru.json`
  - `scripts/tests/i18n-keys.test.mjs`
- 未改动但已核对:
  - `.trellis/spec/frontend/index.md` 在仓库内缺失，本轮按既有代码风格 best-effort 实施。
- 结构化回写提示:
  - 更新固定章节时，优先使用 `update-doc-section`
  - 更新 `## Wave 执行记录` 下的单个 `### Wave N` 时，优先使用 `append-wave-record`

## Wave 执行记录

### Wave 1

- 目标:
  - 恢复“监控/配额”多语言与多客户端快速切换能力，并让最小链路可验证。
- 改动:
  - i18n：补齐 `nav.monitor`、`client_selector.*`、`client_management.*`，并补齐 ru 的 batch download / reload from store 相关文案。
  - 多客户端：登录页接入“选择/管理”；Header 接入“登录后快速切换”。
  - 401 事件：增加请求级抑制开关，切换探测请求不触发全局登出事件。
  - 测试：新增 i18n key 存在性单测，防止再次出现回显 key。
- 结果:
  - `pnpm test` 通过（15/15）。
  - `pnpm run type-check` 通过。
  - `pnpm run build` 通过。

## 验证记录

- 命令:
  - `pnpm test`
  - `pnpm run type-check`
  - `pnpm run build`
- 结果:
  - 单元测试：15/15 通过（包含 `scripts/tests/i18n-keys.test.mjs`）。
  - TypeScript：通过（无新增类型错误）。
  - 构建：通过（Vite 产物生成成功）。
- 说明:
  - `pnpm run build` 执行期间出现两行 `The system cannot find the path specified.` 噪音，但最终构建成功，未观察到失败输出。

## 风险、阻塞与未验证项

- 风险:
  - 浏览器端批量 ZIP 下载在极大文件数量（如 >=2000）时可能触发资源限制；已增加二次确认提示，并在文案中建议改用本仓库本地脚本导出到目录再处理。
- 阻塞:
  - 无。
- 未验证项:
  - 需要你本地手工验收：
    - 切换语言后侧边栏不回显 i18n key（尤其是“监控”菜单）。
    - 登录后从 Header 下拉切换到无效连接时：提示失败但不强制登出/不跳回登录页。
    - 切换到有效连接时：提示成功并刷新生效。

## 验收准备

- 可交给 `devone-acceptance` 的证据:
  - 自动化验证：`pnpm test` / `pnpm run type-check` / `pnpm run build` 均通过。
  - 防回归：已加入 i18n key 存在性测试 `scripts/tests/i18n-keys.test.mjs`。
- 仍需补充的证据:
  - 手工 UI 验收记录（你本地运行 `pnpm dev` 完成，并按上面的未验证项逐条核对）。
