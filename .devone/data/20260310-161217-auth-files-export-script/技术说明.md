# 技术说明

## 文档元信息

- 任务名称: auth-files-export-script
- 任务目录: .devone/data/20260310-161217-auth-files-export-script
- 创建时间: 20260310-161217
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: acceptance
- 当前状态: accepted

## 实际改动摘要

- 本轮改动:
  - 新增 `scripts/export-auth-files.mjs`：直连管理端 API 批量导出认证文件到本地目录（限并发、默认排除 `runtimeOnly`、失败不中断、文件名安全化与路径穿越防护）。
  - 新增 `scripts/tests/export-auth-files.test.mjs`：覆盖 base 推导、文件名安全、并发池上限、Authorization 注入、`runtimeOnly` 默认过滤与轻量 PT-001 property check。
  - 更新 `package.json`：新增 `pnpm test`（`node --test scripts/tests`）。
  - 更新 `.gitignore`：忽略 `auth-files-export/` 输出目录，降低误提交敏感导出文件的风险。
- 涉及文件/模块:
  - `scripts/export-auth-files.mjs`
  - `scripts/tests/export-auth-files.test.mjs`
  - `package.json`
  - `.gitignore`
- 未改动但已核对:
  - 管理端 API 前缀约定为 `/v0/management`（与前端 `computeApiUrl()` 语义一致）。
  - 鉴权 Header 约定为 `Authorization: Bearer <managementKey>`。
- 结构化回写提示:
  - 更新固定章节时，优先使用 `update-doc-section`
  - 更新 `## Wave 执行记录` 下的单个 `### Wave N` 时，优先使用 `append-wave-record`

## Wave 执行记录

### Wave 1

- 目标:
  - 交付一个可复验的导出脚本（方案 A），用于把管理端的 auth files 批量导出到本地目录。
  - 用 Node 内置 test runner 提供最小单测与轻量属性测试，确保并发与路径安全边界可被复验。
- 改动:
  - 增加脚本与单测，补齐 `pnpm test` 入口。
- 结果:
  - `pnpm test` 通过（7/7 tests pass）。
  - `node scripts/export-auth-files.mjs --help` 输出使用说明正常。

### Wave 2（Acceptance / 真实环境导出）

- 目标:
  - 用真实管理端 base 跑一次全量导出，补齐验收门禁证据（不记录明文 key，不在日志中输出文件名）。
- 命令（示例，不含凭证）:
  - `node scripts/export-auth-files.mjs --base http://129.146.34.174:8317/ --out auth-files-export/20260310-171609 --concurrency 4 --timeout-ms 60000 --quiet`
- 结果（Summary）:
  - apiBase: `http://129.146.34.174:8317/v0/management`
  - outDir: `auth-files-export/20260310-171609/`（已被 `.gitignore` 忽略）
  - listed: `11888`
  - candidates: `11888`
  - success: `11888`
  - skippedExisting: `0`
  - skippedRuntimeOnly: `0`
  - failed: `0`
  - ExportedFilesCount: `11888`
- 说明:
  - 使用 `--quiet` 会抑制失败详情（避免在 stdout 打印认证文件名）；需要排障时再显式去掉 `--quiet`。

## 验证记录

- 命令:
  - `node scripts/export-auth-files.mjs --help`
  - `pnpm test`
  - `node scripts/export-auth-files.mjs --base http://129.146.34.174:8317/ --out auth-files-export/20260310-171609 --concurrency 4 --timeout-ms 60000 --quiet`
- 结果:
  - `--help` 输出正常，包含 `--base/--out` 必填项与并发/超时等选项。
  - `pnpm test` 通过（Node TAP 输出显示 7 个子测试均为 ok）。
  - 真实环境全量导出完成：listed=11888，success=11888，failed=0。
- 说明:
  - 单测使用本地 `http.createServer()` 模拟管理端 API，验证 Authorization header、列表接口与下载落盘链路。
  - 真实导出时同样不在仓库/文档/日志中记录任何明文 key（凭证仅通过环境变量注入）。

## 风险、阻塞与未验证项

- 风险:
  - 导出文件属于敏感数据：请把 `--out` 指向仓库外目录，或使用 `*.local` 目录名避免误提交。
  - 凭证注入需谨慎：建议用环境变量 `CPA_MANAGEMENT_KEY`，避免把 key 写入脚本/命令历史/CI 日志。
- 阻塞:
  - 无。
- 未验证项:
  - 无（已完成真实环境全量导出验证；见 `Wave 2` 与 `验证记录`）。

## 验收准备

- 可交给 `devone-acceptance` 的证据:
  - `scripts/export-auth-files.mjs`（脚本实现）
  - `scripts/tests/export-auth-files.test.mjs`（UT-001~UT-003 + PT-001）
  - `pnpm test` 通过的 fresh 输出
- 仍需补充的证据:
  - 无。
